---
phase: 01-shell-auth-storage-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified: [app.js, js/views/signup.js, js/views/login.js, js/views/dashboard.js]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, USER-01]

must_haves:
  truths:
    - "New users can sign up with email and password, validating input formats"
    - "Registered users can log in, writing active session state to localStorage"
    - "Dashboard shows user reputation, questions asked, and answers count"
    - "Logging out clears session state and redirects user to #login"
    - "Unauthenticated users trying to access #dashboard are redirected to #login"
  artifacts:
    - path: "js/views/signup.js"
      provides: "Signup rendering and user insertion database logic"
    - path: "js/views/login.js"
      provides: "Login rendering and session write logic"
    - path: "js/views/dashboard.js"
      provides: "Dashboard renderer, profile stats, and logout handler"
  key_links:
    - from: "js/views/signup.js"
      to: "js/db.js"
      via: "invoking db.insert to save new credentials"
    - from: "js/views/login.js"
      to: "js/db.js"
      via: "checking user credentials and setting active session key"
    - from: "js/views/dashboard.js"
      to: "js/db.js"
      via: "reading profile stats and clearing session keys on logout"
---

<objective>
Implement credentials-based authentication, dashboard profile statistics rendering, and session routing rules.

Purpose: Provide full sign up, sign in, user profiling, and session routing protection.
Output: js/views/signup.js, js/views/login.js, js/views/dashboard.js, and integration into app.js.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-auth-storage-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Sign Up Component</name>
  <files>js/views/signup.js</files>
  <read_first>js/db.js</read_first>
  <action>
    Create `js/views/signup.js` with:
    1. Render function returning the HTML structure for the Sign Up form (glassmorphism card).
    2. Input fields: Username, Email, Password, Confirm Password.
    3. Action submit handler:
       - Validate email structure using standard regex format.
       - Validate password length (minimum 6 characters).
       - Ensure passwords match.
       - Check db if user email already exists.
       - Insert new user into DB collection with default values (reputation: 1, questionsCount: 0, answersCount: 0).
       - Display a dynamic toast notification on success/error.
       - Redirect to `#login` on success.
  </action>
  <verify>Ensure validation rules display proper messages on invalid input</verify>
  <done>js/views/signup.js is created, implements all validator checks, and registers users in localStorage</done>
</task>

<task type="auto">
  <name>Task 2: Build Login Component</name>
  <files>js/views/login.js</files>
  <read_first>js/db.js</read_first>
  <action>
    Create `js/views/login.js` with:
    1. Render function returning the HTML layout for the Login form (glassmorphism card).
    2. Input fields: Email, Password.
    3. Submit form listener:
       - Validate email and password inputs.
       - Query DB users list to check for email matching.
       - Verify password.
       - Set session user token/ID in the DB session space.
       - Trigger dynamic login success toast.
       - Redirect window location hash to `#dashboard`.
  </action>
  <verify>Verify correct credentials allow entry and incorrect credentials trigger an error alert/toast</verify>
  <done>js/views/login.js matches credentials against registered records and saves session state upon login</done>
</task>

<task type="auto">
  <name>Task 3: Build Dashboard and Profile View</name>
  <files>js/views/dashboard.js</files>
  <read_first>js/db.js</read_first>
  <action>
    Create `js/views/dashboard.js` with:
    1. Render function returning the developer dashboard view.
    2. Profile stats cards:
       - Display User Name and Email.
       - Reputation score (represented dynamically, defaults to 1).
       - Questions asked (count of questions from database authored by this user, initially 0).
       - Answers provided (count of answers authored by this user, initially 0).
    3. Recent activity list container. Show empty state placeholder: "No recent activity found. Ask a question or post an answer to get started!"
    4. Logout button handler:
       - Clear session token in database.
       - Trigger toast notification.
       - Redirect window location hash to `#login`.
  </action>
  <verify>Ensure dashboard renders authenticated user metadata and clears session state on logout</verify>
  <done>js/views/dashboard.js renders profile dashboard stats, lists activity placeholder, and logs out user</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Routing and Guards in app.js</name>
  <files>app.js</files>
  <read_first>js/router.js, app.js</read_first>
  <action>
    Modify `app.js` to register the new auth views and routing rules:
    1. Import `SignupView` from `js/views/signup.js`.
    2. Import `LoginView` from `js/views/login.js`.
    3. Import `DashboardView` from `js/views/dashboard.js`.
    4. Define full declarative route map:
       - `#login`: login view rendering, requiresAuth: false
       - `#signup`: signup view rendering, requiresAuth: false
       - `#dashboard`: dashboard view rendering, requiresAuth: true
    5. Update navbar links dynamically based on session status (if user logged in, hide signup/login, show dashboard + logout link in header/sidebar).
    6. Wire global click handler for logout selector to route back to login.
  </action>
  <verify>Verify route guards redirect guests to #login, and logged-in users are redirected to #dashboard when trying to access login page</verify>
  <done>app.js imports views, registers paths, applies auth state validation, and updates navbar display dynamically</done>
</task>

</tasks>

<verification>
- [ ] Direct routing to #dashboard without auth redirects to #login
- [ ] Registering a user successfully updates the localStorage db users array
- [ ] Logging in updates active session keys
- [ ] Dashboard shows username, email, reputation points, and empty activity list
- [ ] Clicking logout redirects user back to #login, wiping local storage session info
</verification>

<success_criteria>
- Sign up page registers and validates credentials
- Login logs in user and persists session across browser reloads
- Dashboard page renders stats and user identity correctly
- Logout clears state cleanly
- Security route guards block unauthenticated entry
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-auth-storage-foundation/01-02-SUMMARY.md`
</output>
