---
phase: 01-shell-auth-storage-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [index.html, index.css, js/db.js, js/router.js, app.js]
autonomous: true
requirements: [DESI-01]

must_haves:
  truths:
    - "Application shell displays in sleek dark mode with glassmorphic elements"
    - "Local database seeds default data into localStorage on initial load"
    - "Hash URL changes trigger routing changes dynamically"
  artifacts:
    - path: "index.html"
      provides: "Main entry point with layout structures and CDN imports"
    - path: "index.css"
      provides: "Core styling, HSL dark mode variables, and layout styles"
    - path: "js/db.js"
      provides: "LocalStorage client-side database wrapper with CRUD and seeding"
    - path: "js/router.js"
      provides: "Client-side SPA hash router with route guarding and transitions"
    - path: "app.js"
      provides: "Application entry point and routing bootstrap"
  key_links:
    - from: "app.js"
      to: "js/db.js"
      via: "import and instantiation of Database class"
    - from: "app.js"
      to: "js/router.js"
      via: "import and instantiation of Router class"
---

<objective>
Setup the core application skeleton including UI shell, localStorage database driver, and client-side hash router.

Purpose: Provide the styling, storage, and navigation foundation for all application views.
Output: index.html, index.css, js/db.js, js/router.js, and app.js.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Main HTML Scaffold</name>
  <files>index.html</files>
  <action>
    Create a standard index.html file containing:
    1. Title: "DevFlow - Developer Q&A Platform"
    2. Meta viewport tags for responsiveness.
    3. CDN integrations:
       - Google Fonts: "Outfit" (weights 300,400,500,600,700) and "Inter" (weights 300,400,500,600)
       - FontAwesome 6.x (CSS stylesheet)
    4. Layout container layout structure:
       - App wrapper: #app
       - Navigation header: #header
       - Layout body: #layout-body (flex/grid container)
       - Sidebar menu: #sidebar
       - Main content container: #main-content
       - Dynamic toast container: #toast-container
    5. Script tag linking `app.js` with type="module".
  </action>
  <verify>Verify layout elements exist in index.html</verify>
  <done>index.html contains linked stylesheets, google font links, layout container elements, and modules script</done>
</task>

<task type="auto">
  <name>Task 2: Define Theme and Layout Styles</name>
  <files>index.css</files>
  <read_first>index.html</read_first>
  <action>
    Implement index.css styling rules:
    1. HSL color variables under :root:
       - --bg-dark (main dark background: e.g. 224 25% 6%)
       - --bg-card (sidebar/header elements: e.g. 224 20% 9%)
       - --accent-primary (indigo/violet color: e.g. 262 80% 50%)
       - --accent-glow (indigo overlay transparent: e.g. 262 80% 50% / 0.15)
       - --text-primary (98% lightness: e.g. 210 40% 98%)
       - --text-secondary (muted text: e.g. 215 20% 65%)
       - --border-glow (semi-transparent border: e.g. 262 80% 50% / 0.3)
       - --border-muted (default subtle border: e.g. 224 20% 15%)
    2. Reset elements (box-sizing, margins, smooth scrolling).
    3. Main layouts: Flexbox for header, CSS Grid for #layout-body (sidebar + #main-content).
    4. Glassmorphism utilities (.glass-card):
       - Background color using hsla (transparent dark overlay)
       - Backdrop-filter blur (12px)
       - Subtle glowing borders and box-shadows.
    5. Transition rules: Add .fade-out { opacity: 0; } and transition: opacity 150ms ease-in-out on #main-content.
    6. Modern button and input styles in dark theme.
  </action>
  <verify>Verify CSS properties and transitions</verify>
  <done>index.css contains HSL dark mode variables, glassmorphic layout styles, and 150ms fade-out transition definitions</done>
</task>

<task type="auto">
  <name>Task 3: Build localStorage Database Wrapper</name>
  <files>js/db.js</files>
  <action>
    Create a modular, exportable database file:
    1. Class `Database` that accepts a storage key.
    2. `load` method returning parsed object from localStorage or null.
    3. `save` method serializing state back to localStorage.
    4. Seeding method `seed` that populates initial data if localStorage is empty:
       - Users: Array with seed accounts:
         * Admin/Expert user (e.g. expert@devflow.com / admin123)
         * Sample Guest user (e.g. guest@devflow.com / guest123)
       - Questions & Answers seed models (empty for now, to be populated in Phase 2)
       - Active user session (null initially)
    5. Helper CRUD methods for collections (e.g., `insert(collection, record)` supporting auto-increment IDs).
    6. Export a singleton instance of the database.
  </action>
  <verify>Test if Database initializes and seeds data when localStorage is cleared</verify>
  <done>js/db.js creates database instance, supports insert/save, and seeds default users list on first load</done>
</task>

<task type="auto">
  <name>Task 4: Build Router and Initialize App Shell</name>
  <files>js/router.js, app.js</files>
  <read_first>js/db.js</read_first>
  <action>
    1. Implement Router class in `js/router.js` that maps hash patterns to rendering functions.
    2. Support route guard callback checking if routes require active sessions.
    3. Handle route changes with 150ms transitions (apply .fade-out, update innerHTML, remove .fade-out).
    4. Bootstrap the application in `app.js`:
       - Import and setup db singleton.
       - Render the persistent header and sidebar UI shell.
       - Configure route maps:
         * `#` (root, redirects to #dashboard or #login depending on auth status)
         * `#404` (not found page)
       - Start router listener.
  </action>
  <verify>Verify application loads and displays the layout shell, responding to hash shifts</verify>
  <done>js/router.js handles route transitions; app.js instantiates database and router, rendering structural shell</done>
</task>

</tasks>

<verification>
- [ ] index.html parses correctly and references app.js
- [ ] index.css sets root HSL variables and handles glass-card rules
- [ ] js/db.js seeds database correctly on clean run
- [ ] Changing URL hash transitions content within 150ms
</verification>

<success_criteria>
- Project structure initialized correctly
- Glassmorphic layout shell renders without console warnings
- Local database seeds users properly in localStorage
- Client-side navigation functions dynamically on hash change events
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-auth-storage-foundation/01-01-SUMMARY.md`
</output>
